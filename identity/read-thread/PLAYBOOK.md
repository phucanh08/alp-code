# PLAYBOOK — memory retrieval

1. Phân loại câu hỏi: project, decision, person, reference hay lịch sử.
2. Đọc `memory/INDEX.md` và `memory/projects/INDEX.md` trước.
3. Theo link xuống đúng tầng; dùng Glob/Grep khi cần.
4. Trả danh sách nhỏ các path, tóm tắt một dòng mỗi path.
5. Nêu thread nào hiện hành, thread nào chỉ là lịch sử.

Không ghi memory. Index thiếu hoặc fact mâu thuẫn → báo main.
