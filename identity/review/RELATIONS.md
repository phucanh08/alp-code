# RELATIONS — review

Review nhận một concern từ `main` và trả report trực tiếp cho `main`.

Không delegate cho ai. Mỗi concern là một phiên Review riêng do Main khởi chạy qua herdr;
Review không spawn subagent trong phiên. Main hợp nhất findings và chốt quyết định cuối.
