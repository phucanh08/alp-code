# Nhật ký — 2026-08

## 2026-08-14
- Khởi tạo Phở. Dựng bộ file identity theo mô hình OpenClaw: `SOUL` / `IDENTITY` / `AGENTS` /
  `USER` / `TOOLS` / `MEMORY` / `HEARTBEAT` / `BOOTSTRAP` + `CLAUDE.md` làm điểm vào.
- Cấu hình chốt: runtime Claude Code, tiếng Việt, tính cách main điềm tĩnh.
- Principal xác nhận tên: **Lê Phúc Anh**.
- Thay `projects/REGISTRY.md` + `memory/projects/` bằng **Project Layer 3 tầng** theo mô hình
  Hermes: L0 `INDEX.md` → L1 `<slug>/PROJECT.md` → L2 `{decisions,log,refs}/`.
  Kiểm soát bằng `modified` — xem [quyết định](../decisions/260814-project-layer-3-tang.md).
- Viết `scripts/sync-project-index.sh`, kiểm thử đủ 5 tín hiệu: MISSING, MISMATCH, DRIFT,
  STALE, ORPHAN (hai chiều) + chế độ `--write`.
- Nghiên cứu **herdr 0.7.4** (đã cài sẵn máy): chạy server headless, kiểm chứng tay rollup,
  `wait`, `--seq`, `events.subscribe`, đo chi phí context từng lệnh; dọn sạch sau khi thử.
- Viết bộ hướng dẫn `docs/herdr/` theo 3 tầng — xem [quyết định](../decisions/260814-herdr-lam-lop-fleet.md).
- `TOOLS.md`: thêm bảng chọn subagent `alp:*` vs herdr, xếp lệnh herdr vào 3 mức quyền.
- Tạo **skill `herdr`** tại `.claude/skills/herdr/`, symlink ra `~/.claude/skills/` để gọi
  được từ mọi repo. Kèm 3 script đã kiểm thử trên fleet 5 agent: `fleet-scan.sh`,
  `fleet-inbox.sh`, `fleet-watch.py` — xem [quyết định](../decisions/260814-skill-herdr.md).
- Lúc kiểm thử script phát hiện 3 điều làm sai tài liệu viết trước đó: `report-agent` không
  nhận `done`; mỗi kết nối socket chỉ subscribe được một lần; tên event không thống nhất
  ngay trong cùng stream. Đã sửa vào `docs/herdr/gotchas.md` §8–8c.
- Còn treo: danh sách project thật, nhịp báo cáo định kỳ, chưa thử herdr với agent thật (mới
  dùng tiến trình giả để tránh tốn token).

### Phiên chiều — thử skill herdr với Claude thật
- Chạy 2 agent `claude` thật trong pane herdr. Nhận diện **không cần cài integration hook**:
  tự nhận `agent: claude`, `interactive_ready`, bắt đúng `blocked` khi Claude hỏi trust-folder.
- `agent explain` chạy tốt với agent thật — in rule khớp (`live_blocked_form`, priority 980),
  region, kèm trích màn hình làm bằng. Đây là thứ lượt trước không kiểm chứng được.
- 3 script của skill chạy đúng trên fleet thật, **không phải sửa dòng nào**.
- `fleet-watch.py` bắt trọn chuỗi `blocked` → (Enter) → `working` → `done` của agent thật.
- **herdr tự update 0.7.4 → 0.8.0 giữa phiên** (protocol 16→19), làm tài liệu viết buổi sáng
  sai nhiều chỗ: xoá cả nhóm `herdr wait`, bỏ `agent send`, `agent start` đổi sang
  `--kind KIND --pane ID` và cần pane đã ở dấu nhắc shell. Đã cập nhật toàn bộ docs + SKILL.md
  + TOOLS.md, thêm bẫy §0 và §0b vào `gotchas.md`.
- Học được: `agent prompt --wait` là primitive gọn nhất cho vòng lặp batch (gửi + chờ trạng
  thái ổn định trong một lệnh) — đã chạy thật, 11.6s, exit 0.
- Ghi thêm luật vào SKILL.md: gặp `unknown option` thì kiểm tra `herdr --version` trước khi
  debug; tin `herdr <nhóm> <lệnh> --help` hơn dòng tóm tắt của nhóm.

### Phiên chiều — model routing
- Khảo sát model hai runtime: Claude (skill `claude-api`) và Codex CLI 0.147.0
  (`~/.codex/models_cache.json`, fetch cùng ngày). Viết `docs/model-routing.md`.
- Phát hiện ràng buộc: `Agent` tool **chỉ nhận 4 alias** `opus`/`sonnet`/`haiku`/`fable`,
  không spawn được subagent trên model ID cụ thể.
- **Báo sai một lần:** bản đầu lấy giá Terra/Luna từ blog bên thứ ba → sai (Terra 2.50/15,
  Luna 1/6). Docs OpenAI cho 2.00/12 và 0.20/1.20 — lệch 80% ở Luna. Nguyên nhân: blog dùng
  giá ra mắt, và chính trang công bố GPT-5.6 của OpenAI cũng chưa cập nhật; chỉ trang pricing
  là đúng. Đã sửa và ghi luật "ưu tiên docs OpenAI" vào `USER.md` + mục 7 của doc.
- Principal chốt luật phân chia: **Codex** cho nghiên cứu sâu + khi cần phương án sáng tạo
  hơn; **Claude Code** mọi việc còn lại. Chia theo loại việc, không theo độ khó.
  Xem [quyết định](../decisions/260814-phan-chia-codex-claude-code.md).
- Principal chốt mọi skill dùng chung (`herdr` hiện tại và skill tương lai) phải có một
  canonical store cố định, trung lập runtime; Claude Code và Codex cùng trỏ tới nguồn này.
  Hook riêng từng runtime để nghiên cứu và chốt sau. Chưa migration file vật lý trong phiên.
- Ngân sách: **Sol cứ dùng**; **Fable 5 hỏi trước mỗi lần**, kèm lý do vì sao Opus 5 không đủ.
- Ghi vào 3 tầng: `USER.md` (ràng buộc lặp lại) · `TOOLS.md` §2 luật + §4 quyền duyệt ·
  `docs/model-routing.md` (chi tiết).
- Tự rút ra cho prompt gửi subagent: Opus 5 **tự verify sẵn** → mọi lệnh "hãy kiểm tra lại"
  đều phản tác dụng; nó cũng tự mở rộng scope, viết dài, và delegate hăng hơn 4.8. Ghi ở
  mục 5 của doc.

### Phiên chiều — migration canonical skill store
- Di chuyển nguyên trạng `herdr` từ `.claude/skills/herdr/` sang canonical store
  `skills/herdr/`; giữ nguyên checksum và permission của `SKILL.md` cùng ba script.
- Đổi `~/.claude/skills/herdr` và tạo `~/.agents/skills/herdr` thành symlink tới cùng
  `/Users/oaidq/AnhlpProjects/agent-memory/skills/herdr`.
- Không tạo bản sao trong `~/.codex/skills`, không cài integration hook. Catalog runtime sẽ
  được kiểm chứng trong phiên Claude Code/Codex mới kế tiếp.

## 2026-08-21 — Migrate sang `agent-memory`, thêm vai thứ hai

Chuyển toàn bộ `agent-team/pho` → repo `agent-memory` với kiến trúc identity-theo-vai.
Chi tiết quyết định: `memory/shared/decisions/260821-agent-memory-architecture.md`.

**Đã làm:**
- Tách `AGENTS.md` + `USER.md` + `TOOLS.md` cũ thành `_shared/{PRINCIPAL,VOICE,HOUSE-RULES,
  DELEGATION,CONVENTIONS}.md` (dùng chung) và `main/{PLAYBOOK,RELATIONS}.md` (riêng vai).
- `loadout.yaml` thành nguồn ACL duy nhất; `compile-acl.sh` sinh `.claude/settings.json`.
- Ba hook: `session-start` (nạp identity, lọc INDEX theo quyền), `acl-guard` (chặn Bash lách
  ACL), `session-end` (nhắc ghi nhớ). `doctor.sh` gộp 9 loại tín hiệu toàn vẹn.
- Thêm vai `researcher` (Long 🔎) qua `new-role.sh` — 0.86s.

## 2026-08-21 — Đổi tên alp-code, tách Knowledge Retrieval

Đổi tên project thành `alp-code`. Retire vai `researcher`; thay bằng Search
(`gpt-5.6-terra`, local code), Librarian (`gpt-5.6-sol`, external/cross-repo) và Read Thread
(`gpt-5.6-luna`, memory). Thêm launcher Codex read-only đa nền tảng `scripts/run-role.*`.

**Spike ACL cho 4 phát hiện đổi kiến trúc** (`shared/reference/claude-code-acl-behavior.md`):
absolute path trong permission rule phải viết `//` · workspace chưa trusted thì `allow` và
`additionalDirectories` bị bỏ qua im lặng · `deny` **sống sót** qua `bypassPermissions`
nhưng không chặn được `Bash` · hook `PreToolUse` fire ở mọi mode.

**Nghiệm thu:** `test-isolation.sh` 20/20 · boot set ~4.2k token/vai · đổi `name:` chỉ tốn
1 dòng, 0 recompile · `doctor.sh` sạch.

**Còn treo:** 4 câu hỏi mở trong plan (vai tiếp theo, git-track `private/`, review queue cho
`refs/`, số phận `agent-team/`) — chờ principal.
