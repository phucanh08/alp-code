# Quét sâu vr-mobile-app — 2026-08-14

3 agent herdr (`vr-core`, `vr-ui`, `vr-ops`) quét song song 3 mảng không giao nhau, cwd = repo,
read-only. Phở đã đọc lại và spot-check các claim nặng trước khi tin.

| Mảng | Report | Dòng |
|---|---|---|
| Core: bootstrap, dio, bloc, router, session, SignalR/FCM, repo, code health | `plans/reports/scan-core-260814-1526-vr-mobile-app.md` | 388 |
| UI: 10 module / ~57 màn, luồng nghiệp vụ, design system, tech debt | `plans/reports/scan-ui-260814-1526-vr-mobile-app.md` | 270 |
| Ops: build/release, permission, key, package native, test, CI, git | `plans/reports/scan-ops-260814-1526-vr-mobile-app.md` | 420 |

## Phở đã tự xác minh
- `AnalyticsConfig.secretKey` literal + call site + bản `fromEnvironment` bị comment — đúng.
- `DebugLoggingInterceptor` add ngoài `kDebugMode`, `DebugLogStore.clear()` không call site — đúng.
- `DevToolDialog` sau 20 tap, không guard `kDebugMode` — đúng. (Trả lời câu hỏi mở #7 của `vr-core`.)
- 18 file tracked trong `ios/Runner 2026-06-*/`, gồm 6 IPA — đúng.
- `docs/`+`plans/` bị gitignore, không có CI config nào — đúng.
- 3 file lớn nhất UI, route chết `/signup/personal/{scan,phone,otp}` — đúng.

## Lệch so với report agent
- `vr-ui` đếm `print(` = 21 → thực đo 16. `Color(0x…)` = ~470 → thực đo 752 lần xuất hiện.
- `vr-core` nói `DebugLogStore` "không bao giờ clear" → chính xác là **có hàm `clear()` nhưng
  không nơi nào gọi**. Hệ quả giống nhau.
- Con số đếm của agent nói chung **không tin được**; kết luận cấu trúc thì tin được.

## Câu hỏi mở còn lại (không tự trả lời được từ repo)
1. 2 key `AIzaSy…` đã restrict trên Google Cloud Console chưa?
2. Bản nào đang chạy trên store? Không có tag release nào.
3. Quan hệ `dev` → `main` → `production` → `epay/dev`; nhánh nào phát hành thật?
4. Profile team `9MWNUFY2N6` (6 IPA tháng 6) đã thu hồi chưa?
5. Có CI ở group cha GitLab `epay/vietnam_register` không?
6. `packages/ekyc_sdk/sdk-native/` (72 MB) còn cần không, hay chỉ là bản vendor giao?
