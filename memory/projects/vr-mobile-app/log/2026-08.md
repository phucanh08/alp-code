# Log 2026-08 — vr-mobile-app

## 2026-08-14 — Quét sâu, chốt PAUSED
3 pane herdr quét repo; dựng L1 card; phát hiện 4 lỗ P0 (secret hardcode, DebugLoggingInterceptor
ở release, dev tool 20-tap, 6 IPA tracked). Principal chốt PAUSED, không đụng repo.
Chi tiết: `refs/scan-260814.md`.

## 2026-08-17 — Mở lại: màn nhập CCCD của luồng đăng ký cá nhân
Principal yêu cầu sửa `signup_personal_id_card_page.dart`.

Tình trạng nhận vào: file mới thêm (`AM` trong git status), **không compile được** — copy-paste
dở từ `signup_personal_password_page.dart`. Thiếu toàn bộ state (`passwordController`,
`confirmController`, `pwError`, `confirmError`, `_isLoading`, `_handleConfirm`), thiếu import
`flutter/services.dart`, hai ô nhập CCCD/họ tên để `obscureText: true`, tiêu đề vẫn là
"Khởi tạo mật khẩu". Màn là orphan — không GoRoute, không màn nào push tới.

Đã làm:
- Viết lại state + validate: CCCD digitsOnly, đúng 12 số; họ tên trim, không rỗng, max 100.
  Lỗi chỉ hiện sau khi user chạm field (`_touched*`), submit thì ép hiện cả hai.
  Wording lỗi lấy trùng `signup_personal_manual_page.dart` cho nhất quán.
- Bàn phím số dùng `safeNumericKeyboardType` (workaround iPadOS 26 đã có sẵn trong repo).
- "Tiếp theo" → `context.push('/signup/personal/password', extra: {cccd, fullName})`.
- Thêm `GoRoute('/signup/personal/id-card')` vào `signup_personal_flow.dart`.

Quyết định của principal trong phiên:
- "Màn đặt lại mật khẩu" = `/signup/personal/password` (SignupPersonalPasswordPage), **không phải**
  `/forgot-password/new-password`. Lý do: màn đích nhận đúng `userData['cccd']` +
  `userData['fullName']` để gọi `AuthService.register()`.
- Thêm route nhưng **chưa nối điểm vào** — principal tự nối sau.

Kiểm chứng: `flutter analyze lib/ui/pages/auth/presentation/signup/personal/` → 0 issue ở 2 file
đã sửa; 7 issue còn lại có sẵn ở `otp_page`, `phone_page`, `manual_page`. Chưa chạy app thật.

Chưa commit (chưa được yêu cầu). Working tree còn nhiều file dirty từ trước, không phải của Phở.

## 2026-08-17 (tiếp) — QR CCCD cho scanner + nối vào màn nhập CCCD

`identity_qr_code_scanner_dialog.dart` (trước đó là dead code, 0 caller):
- Thêm `CitizenIdData` + `parseCitizenIdQr()`. Định dạng QR mặt trước CCCD gắn chip:
  7 trường ngăn `|` — `CCCD | CMND cũ | họ tên | ngày sinh | giới tính | địa chỉ | ngày cấp`.
  CCCD 12 số; CMND cũ rỗng hoặc 9 số; ngày dạng `ddMMyyyy` (kiểm ngày có thật bằng `DateTime`
  rồi so lại d/m/y vì `DateTime` tự cuộn 31/02 → 03/03).
- Tên field khớp `EkycCardReader._mapNfcData` (`cccd`/`fullName`/`dob`/`gender`/`address`/
  `issue_date`) để luồng QR và luồng NFC ra cùng cấu trúc.
- Thêm `QrScanTarget {vehicle, citizenId}` + `showCitizenId()`; `show()` cũ giữ nguyên chữ ký.
  Tách `_rejectScan()` dùng chung. Tiêu đề đổi theo target.

`signup_personal_id_card_page.dart`:
- Thêm card "Quét QR Căn cước công dân" + divider "Hoặc nhập thủ công", copy kiểu từ
  `verify_account_page.dart:182` (dùng `minimumSize: Size.zero`, không dùng `minSize` deprecated).
- Quét xong điền sẵn 2 ô; giữ `_scanned` để kèm dob/gender/address/issue_date vào `extra`,
  **chỉ khi** user chưa sửa tay hai ô đó.

Kiểm chứng: `flutter analyze` 2 file → id_card page "No issues found"; dialog còn đúng 1 issue
`minSize` deprecated **có sẵn từ trước** (dòng 229). `flutter test` parser 3/3 pass (mẫu thật,
biến thể có CMND 9 số, 8 chuỗi rác bị loại) — file test là tạm, đã chuyển ra scratchpad vì chứa
CCCD + địa chỉ thật của principal, không để trong repo 11 người dùng chung. Chưa chạy app thật.

Còn treo: có giữ test parser trong `test/` (viết lại bằng dữ liệu giả) không.

## 2026-08-17 (tiếp) — Màn quên mật khẩu + nhánh `feat/auth-qr-identity-card`

`forgot_password_identity_card_page.dart` (principal tự tạo bằng cách copy màn signup):
- Sửa chữ hiển thị cho đúng luồng: header "Quên mật khẩu", tiêu đề "Xác thực căn cước
  công dân", mô tả nói rõ mục đích đặt lại mật khẩu.
- **Sửa thêm ngoài yêu cầu (đã báo principal):** nút "Tiếp theo" đang push
  `/signup/personal/password` — tức nhảy vào luồng đăng ký. Đổi cho khớp bản NFC
  (`forgot_password_flow.dart:84-93`): `notActivated` → `/forgot-password/new-password`
  (`extra {citizenId}`); `activated`/`unknown` → `/forgot-password/confirm-account`
  (`extra` là String). Bỏ `_buildUserData()` + `_scanned` theo.
- Principal chốt: **màn này bỏ ô họ tên**, chỉ nhập số CCCD. Đã gỡ controller/validate/field.

Principal duyệt commit → tạo nhánh `feat/auth-qr-identity-card` từ `dev` (`197b36d`), 2 commit:
- `79d03f9` feat(auth) — 13 file lib/, toàn bộ luồng QR căn cước.
- `b9f4c3c` chore(release) — cấu hình build stg 1.0.0+22 (env prod→stg, bundle id `.stg`,
  iOS target 14→15, CocoaPods 1.17.0). Tách riêng để principal drop được nếu không muốn.

**Chưa push, chưa merge.** `flutter analyze lib` → 0 error (168 info/warning có sẵn từ trước).

Release note stg 1.0.0+20 → 1.0.0+22:
`pho/plans/reports/release-note-260817-1411-vr-mobile-app-stg-1-0-0-22.md`.

Lưu ý phát hiện khi soạn note: nâng iOS deployment target 14.0 → 15.0 loại thiết bị iOS 14 —
chưa rõ có chủ ý; khối comment giải thích key NFC trong `Info.plist` bị xoá (key vẫn còn).

## 2026-08-19 — Release prod 0.0.4+26081910

Theo yêu cầu principal, tạo nhánh `chore/release-prod-0.0.4` từ `dev`, commit và push cấu hình
release prod trong 4 file: môi trường prod, version `0.0.4+26081910`, splash version và iOS
bundle ID `vn.gov.vr.app`. Commit `e794a9a` đã khớp `origin/chore/release-prod-0.0.4`.

Kiểm chứng: `git diff --check` pass. `flutter analyze` fail với 180 issue có sẵn; 8 error nằm
trong các package example thiếu dependency/entrypoint, không thuộc 4 file release. Working tree
sạch sau push.
